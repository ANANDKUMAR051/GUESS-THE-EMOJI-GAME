fetch("/api/leaderboard").then(r=>r.json()).then(data=>{const box=document.getElementById("table");box.innerHTML=data.length?data.map(x=>`<div class="leaderboard-entry"><span class="rank">#${x.rank}</span><span><b>${x.username}</b><small style="display:block;color:#9ca6bd">${x.gamesPlayed} games · ${x.wins} wins</small></span><b>${x.score} pts</b></div>`).join(""):"<div class='leaderboard-entry'>No players yet.</div>"})
